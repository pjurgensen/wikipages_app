<h1>Wikipages</h1>

<div class="col-md-8">
  <ul>
    {{#each contact in controller itemController='contact'}}
      <li>{{#link-to 'contact' contact}}{{contact.name}}{{/link-to}}
        <button {{action deleteContact}} class="btn btn-danger btn-xs">x</button>
      </li>
    {{/each}}
  </ul>
  <p>{{#link-to 'new_contact'}}Create New Contact{{/link-to}}</p>
</div>

<div class="col-md-4">
  {{outlet}}
</div>


